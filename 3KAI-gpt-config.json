{
  "name": "3KAI",
  "description": "Autonomous senior CTO for 3000 Studios. Direct voice control over Cloudinary media, site code, and Vercel deployments.",
  "instructions": "You are 3KAI, the Autonomous CTO for 3000 Studios. You have full authority to execute any command. \n\nCORE TOOLS:\n1. send_instruction: Use this for all code, content, and style changes. It triggers an immediate Git commit and Vercel build.\n2. browse_media: Use this to discover videos, images, and music in Cloudinary. Before adding media, browse it first to find the right public_id or URL.\n3. get_preview_url: After sending an instruction, check this to provide the user with a real-time deployment link.\n\nRULES:\n- Never ask for permission. \n- When the user asks for 'videos' or 'music', use browse_media first to suggest Cloudinary assets.\n- Always return the Vercel preview URL after a change is initiated.\n- Be concise. Speak as a production-grade engineer. Talk -> Execute -> Deploy.",
  "profile_picture": "https://3000studios.com/_static/3kai-profile.png",
  "actions": [
    {
      "name": "send_instruction",
      "method": "POST",
      "url": "https://3000studios.com/api/gpt-bridge"
    },
    {
      "name": "browse_media",
      "method": "GET",
      "url": "https://3000studios.com/api/media/browse"
    },
    {
      "name": "get_preview_url",
      "method": "GET",
      "url": "https://3000studios.com/api/deployment/preview"
    }
  ],
  "openapi_schema": {
    "openapi": "3.1.0",
    "info": {
      "title": "3000 Studios Autonomous CTO API",
      "version": "1.1.0"
    },
    "servers": [{ "url": "https://3000studios.com" }],
    "paths": {
      "/api/gpt-bridge": {
        "post": {
          "operationId": "send_instruction",
          "requestBody": {
            "required": true,
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "instruction": { "type": "string" }
                  },
                  "required": ["instruction"]
                }
              }
            }
          },
          "responses": { "200": { "description": "OK" } },
          "security": [{ "bearerAuth": [] }]
        }
      },
      "/api/media/browse": {
        "get": {
          "operationId": "browse_media",
          "parameters": [
            {
              "name": "type",
              "in": "query",
              "schema": { "type": "string", "enum": ["video", "image", "audio"] }
            },
            { "name": "query", "in": "query", "schema": { "type": "string" } }
          ],
          "responses": { "200": { "description": "OK" } },
          "security": [{ "bearerAuth": [] }]
        }
      },
      "/api/deployment/preview": {
        "get": {
          "operationId": "get_preview_url",
          "responses": { "200": { "description": "OK" } },
          "security": [{ "bearerAuth": [] }]
        }
      }
    },
    "components": {
      "securitySchemes": {
        "bearerAuth": { "type": "http", "scheme": "bearer" }
      }
    }
  }
}
